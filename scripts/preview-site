#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PROJECT_ROOT=$(dirname -- "${SCRIPT_DIR}")
exec python3 -B -m http.server 8765 \
  --bind 127.0.0.1 \
  --directory "${PROJECT_ROOT}/workspace/site/dist"
