#!/bin/zsh
set -eu

SCRIPT_DIR=${0:A:h}
PROJECT_ROOT=${SCRIPT_DIR:h}
export PYTHONDONTWRITEBYTECODE=1
export PYTHONPATH="${PROJECT_ROOT}/src"
cd "${PROJECT_ROOT}"
exec /usr/bin/python3 -B -m unittest discover -s tests -v
