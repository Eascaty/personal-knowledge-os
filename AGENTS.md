# Project Operating Rules

本目录是个人知识体系项目的唯一工作根目录。后续实现、配置、设计调整、测试与运维记录都应保存在本目录内。

## 强制更新规则

每次发生实质变更后，执行者必须：

1. 更新 `STATUS.md` 中的当前状态、最近验证结果与下一步。
2. 在 `logs/operations.md` 追加一条带 Asia/Shanghai 时间的操作记录。
3. 架构、数据结构、工具选择或安全边界变化时，同步更新 `docs/knowledge-system-design.md`。
4. 产生新的长期取舍时，在 `logs/decisions.md` 中增加决策项。
5. 专业母子链路变化时，先更新 `config/taxonomy.json`，再重建派生产物。

## 安全边界

- 不在文档、日志、Git 或网站产物中记录密码、Cookie、验证码、API Token、完整银行卡号或其他凭据。
- 原始资料只追加，不由 AI 覆盖。
- 只有 `exports/public/` 或明确生成的公开构建允许对外发布。
- 默认所有知识为 `private`。
- 账号导出、原始聊天、数据库、缓存和隔离文件不得进入公开构建。
- 不使用付费 API、付费 SaaS 或有按次费用的服务，除非用户明确改变这一要求。
- 不绕过登录、验证码、付费墙、DRM 或网站访问控制。

## 分类规则

- 专业目录是严格的父子树。
- 一级至三级骨架由用户定义并锁定。
- AI 仅可在允许层级以下创建子节点。
- 每份资料只有一个主路径，但可拥有多个辅助关系。
- 跨领域关系不得改变主目录结构。
- 不确定内容进入“待归类”，不能阻塞其他任务。

## 工程规则

- 核心数据使用开放格式：Markdown、JSON、SQLite、SVG、GraphML。
- 本地流程必须幂等；同一输入重复执行不得产生重复资料。
- 每个阶段可独立重试，单条失败不得阻塞队列。
- 所有自动生成内容必须记录来源、内容哈希、模型版本和提示词版本。
- 不自动升级依赖或模型；升级前先运行固定样本测试。
- 网站发布前必须通过断链、隐私、分类、构建和在线健康检查。
- 发布失败时保留上一线上版本。

## 文档定位

`docs/knowledge-system-design.md` 是当前方案的权威活文档；代码和配置若与文档不一致，应在同一次变更中消除差异。
